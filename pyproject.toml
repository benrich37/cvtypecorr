[project]
name = "research"
dynamic = ["version"]
requires-python = ">=3.9"
authors = [{ name = "Ben Rich", email = "benjamin.rich@colorado.edu" }]


#TODO: Figure out what version we want for ipykernel
dependencies = [
    "janaf"
]